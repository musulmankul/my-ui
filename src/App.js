import Button from "./components/Button";
function App() {
  return (
    <div>
      <Button size ="sm">Confirm1</Button>
      <Button variant="bordered">Confirm</Button>
      <Button size="lg">Cancel</Button>
    </div>

  );
}

export default App;
